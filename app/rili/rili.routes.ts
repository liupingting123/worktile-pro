import { Route } from '@angular/router';

import { RiliComponent } from './rili.component';

export const RiliRoutes: Route[] = [
    {
        path: 'rili',
        component: RiliComponent
    }
];
